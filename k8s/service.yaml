apiVersion: v1
kind: Service
metadata:
  name: billy-voice
  namespace: agent-sandbox
  labels:
    app: billy-voice
spec:
  selector:
    app: billy-voice
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30880
  type: NodePort
